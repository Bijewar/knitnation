service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload images
    match /productImages/{imageName} {
      allow read, write: if request.auth != null;
    }
  }
}
